export const WORKFLOW_GUIDANCE = `- Use workflow tools only when you are taking an application-level action rather than reading code.
- In Spec mode, ask clarifying questions naturally before staging or writing a spec artifact.
- Stage requirements before writing files unless the user explicitly asked you to create/update the artifact now.
- Use \`get_spec_state\` before assuming there is an active spec or task list.
- When the user wants to execute a task, tell them to use the task controls in the spec UI unless the application exposes a dedicated task-start tool.`;
